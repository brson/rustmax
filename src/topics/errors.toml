# Common compiler errors

[topics.e0382]
name = "E0382: borrow of moved value"
aliases = ["moved value", "use after move", "value moved here", "E0382"]
category = "error"
tags = ["ownership", "borrow-checker"]
brief = "Attempting to use a value after it has been moved"
relations = [
    { kind = "requires", target = "ownership" },
    { kind = "requires", target = "move-semantics" },
]

[topics.e0502]
name = "E0502: cannot borrow as mutable"
aliases = ["already borrowed", "mutable borrow conflict", "E0502"]
category = "error"
tags = ["borrowing", "borrow-checker"]
brief = "Cannot borrow as mutable because it is also borrowed as immutable"
relations = [
    { kind = "requires", target = "borrowing" },
]

[topics.e0597]
name = "E0597: does not live long enough"
aliases = ["lifetime too short", "dropped while borrowed", "E0597"]
category = "error"
tags = ["lifetimes", "borrow-checker"]
brief = "A borrowed value does not live long enough"
relations = [
    { kind = "requires", target = "lifetimes" },
    { kind = "requires", target = "borrowing" },
]

[topics.e0277]
name = "E0277: trait bound not satisfied"
aliases = ["trait not implemented", "missing trait", "E0277"]
category = "error"
tags = ["traits"]
brief = "A type does not implement a required trait"
relations = [
    { kind = "requires", target = "traits" },
]

[topics.e0308]
name = "E0308: mismatched types"
aliases = ["type mismatch", "expected type", "E0308"]
category = "error"
tags = ["types"]
brief = "Expected one type, found another"
relations = []

[topics.e0499]
name = "E0499: cannot borrow as mutable more than once"
aliases = ["multiple mutable borrows", "E0499"]
category = "error"
tags = ["borrowing", "borrow-checker"]
brief = "Cannot have multiple mutable borrows of the same value"
relations = [
    { kind = "requires", target = "borrowing" },
]

[topics.e0515]
name = "E0515: cannot return reference to local variable"
aliases = ["dangling reference", "return local reference", "E0515"]
category = "error"
tags = ["lifetimes", "borrow-checker"]
brief = "Cannot return a reference to a value that will be dropped"
relations = [
    { kind = "requires", target = "lifetimes" },
    { kind = "requires", target = "borrowing" },
]
